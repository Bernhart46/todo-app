import { InterfaceProps } from "./interface-types";

export const SortTodosInterface = (props: InterfaceProps) => {
  const { groupName, setIsToggled } = props;

  return <div className="todo-sort__container"></div>;
};
